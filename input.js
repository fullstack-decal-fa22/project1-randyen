function check() {
    const submission = document.getElementById('submission');
    if (submission.value === "Kaguya Shinomiya") {
        alert("That's correct!");
    } else {
        alert("Sorry, try again.")
    }
}
